#!/usr/bin/python
import sys
import os

try:
    import bomb
except ImportError:
    sys.path.insert(0, os.sep.join([os.path.dirname(__file__), '..']))

if __name__ == '__main__':
    from bomb import run
    run()